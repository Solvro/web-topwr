import type { LogLevel } from "./types/internal";

export const API_LOGS_ROUTE = "/api/logs";

export const DEFAULT_LOG_LEVEL: LogLevel = "info";

export const MAX_LOG_PAYLOAD_LENGTH = 1000; // characters
