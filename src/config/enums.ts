export enum ApplicationError {
  Unauthorized = 401,
  Forbidden = 403,
  NotFound = 404,
  ServerError = 500,
  NotImplemented = 501,
}

export enum ImageType {
  Logo = "logo",
  Banner = "banner",
}
