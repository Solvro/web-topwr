/** Used as the initially-selected color in the color picker input. */
export const DEFAULT_INPUT_COLOR = "#ffffff";

/** The base path for the administrative dashboard. */
export const ADMIN_PATH = "/admin";
